apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: testbcklapilistener
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: testbcklapilistener
    spec:
      containers:
      - name: testbcklapilistener
        image: testbackendacr.azurecr.io/listener:1.0.0
        ports:
        - containerPort: 8089
        env:
          - name: SPRING_PROFILES_ACTIVE
            value: test
        resources:
          requests:
            cpu: 250m
          limits:
            cpu: 500m		
      imagePullSecrets:
      - name: denkerskey
      
---
apiVersion: v1
kind: Service
metadata:
  name: testbcklapilistener
spec:
  type: LoadBalancer
  ports:
  - port: 8089
  selector:
    app: testbcklapilistener
